%==============================================================================
\chapter{The monitor target curve}\label{ch:target}
%==============================================================================

The correction SuperMoTo designs aims the loudspeaker at a target. The ideal correction objective deigned in the analysis and group analysis panes correspond to a flat and horizontal frequency response curve. The Target view lets you aim at something else, which almost always looks like a gentle downward tilt.

\section{Why a flat in-room response is the wrong target}\label{sec:whytilt}
A loudspeaker whose direct sound is flat does not measure flat in a room. Its \gterm{di}{directivity index} rises with frequency (ITU-R BS.1116-3 requires a monitor's to, smoothly, between \SI{6}{\dB} and \SI{12}{\dB} over \SIrange{500}{10000}{\hertz} \cite{ITU1116}) so the reverberant share of a steady-state, spatially averaged measurement falls as frequency rises, and absorption in a treated room takes more off the top. The ear follows the direct sound plus the first arrivals and the microphone average follows the steady state. Correct the steady state to flat and the direct sound has been lifted in the treble by the difference. This results in a "harsh" sound.

EBU Tech 3276's operational room response mask holds its upper limit flat at \SI{+3}{\dB} but lets the lower one fall at \SI{1}{\dB} per octave above \SI{2}{\kilo\hertz}, reaching \SI{-6}{\dB} at \SI{16}{\kilo\hertz} \cite{EBU3276}. ITU-R BS.1116-3's widens downwards at \SI{1.5}{\dB} per octave over the same range \cite{ITU1116}. Preference work puts the middle of the range lower still: the Harman in-room target falls about \SI{1}{\dB} per octave across the band with a low shelf under \SI{105}{\hertz} \cite{Olive2013}. Toole's survey of the whole question is the long version \cite{Toole2015}.

A total of \SIrange{-3}{-6}{\dB} from \SI{20}{\hertz} to \SI{20}{\kilo\hertz} is therefore a conservative reading of all three, and is where the factory curves sit.

\paragraph{Not the cinema X-curve.} SMPTE ST 202 and ISO 2969 specify \SI{3}{\dB} per octave above \SI{2}{\kilo\hertz} for rooms over \SI{125}{\cubic\metre}. That is a very different target. Allen's account of where it came from also records what happened when small, dry mix rooms were tuned to it in the 1980s, and material sounded dull there, engineers compensated, so the mixes came out bright \cite{Allen2006}. A small room needs less tilt than a cinema, and the useful idea to borrow is that the turnover frequency, not the slope, is what should move with room size and listening distance.

\section{Choosing how much tilt}\label{sec:tiltchoice}
Section~\ref{sec:whytilt}'s argument is about a room in general. In a particular room, how much of what a measurement picks up is reverberant rather than direct sets how much tilt is actually owed, and that balance is set by the room and by where the listening position sits in it, not by the loudspeaker.

\paragraph{Rule of thumb.} The drier and closer, the flatter; the livelier and further, the steeper. A small, well-treated room with the listening position close to the speakers, as near-field monitoring intends, keeps the direct sound dominant well into the treble, and there is little for a room to add beyond what Section~\ref{sec:whytilt} already accounts for from \gterm{di}{directivity} alone. Flat or Gentle usually reads right. A larger or more reflective room, a listening position further back, or a wide correction area, lets the reverberant field's share grow, and its spectrum falls with frequency faster than the direct sound's, for the same reason. Strong or Harman-like tends to read right there. This is one reading of Olive, Welti and McMullin's finding that listeners preferred a downward-tilting in-room target to a flat one \cite{Olive2013}: the more of the room is in what they heard, the more welcome the tilt.

\paragraph{A number to hang it on.} Room acoustics has a standard quantity for exactly this balance: the \emph{critical distance} $d_c$, the distance from the loudspeaker at which direct and reverberant sound pressure are equal. A common estimate, combining Sabine's reverberation-time formula with the classical room-constant approximation for a diffuse field, is
\begin{equation}
  d_c \;\approx\; 0.057\sqrt{\dfrac{Q\,V}{T_{60}}} \quad \text{(metres, $V$ in \si{\cubic\metre}, $T_{60}$ in seconds)},
  \label{eq:criticaldistance}
\end{equation}
where $Q$ is the loudspeaker's directivity factor in the band of interest (roughly 2--4 for a small monitor's tweeter) and $T_{60}$ the room's reverberation time. At a listening distance $r$, the direct-to-reverberant ratio there is approximately $20\log_{10}(d_c/r)\,\si{\dB}$: well positive favours Flat or Gentle, well negative favours Strong or Harman-like, and close to \SI{0}{\dB} is squarely Moderate, the default.

Two rooms of the same \SI{50}{\cubic\metre} and the same \SI{1.2}{\metre} listening distance, differing only in treatment, illustrate the swing. Well treated, $T_{60}=\SI{0.25}{\second}$, Equation~\ref{eq:criticaldistance} gives $d_c\approx\SI{1.4}{\metre}$, about \SI{+1.3}{\dB}: direct sound leads, mildly. Untreated, $T_{60}=\SI{0.6}{\second}$, $d_c\approx\SI{0.9}{\metre}$, about \SI{-2.5}{\dB}: the same chair, in the same room, is now on the reverberant side. Nothing else moved; only how much of the room was absorbed.

The approximation folding the room constant into the Sabine absorption understates $d_c$ once the average absorption coefficient is no longer small, so it errs, if anything, towards recommending more tilt in a live room rather than less. $Q$ is frequency-dependent and hard to pin down without a polar measurement, and a small room's low end rarely reaches the diffuse-field assumption behind Equation~\ref{eq:criticaldistance} at all. So treat the result as an order of magnitude, one input among others, not a setting to compute and forget.

$T_{60}$ is measured by SuperMoTo. Load a set of measurements in the Analysis pane with the swept-sine estimator, choose the raw impulse view, and the read-out beside it gives the room's reverberation time. You can get a tooltip showing the per octave band values by moving the mouse over it (Section~\ref{sec:rawir}). That is the number Equation~\ref{eq:criticaldistance} wants, from the room the correction was designed in, and the single figure it reports (the mean of the \SI{500}{\hertz} and \SI{1}{\kilo\hertz} octaves) is the one to put in it.

What is not measured is $V$ and $r$: a tape measure settles both, and $Q$ stays an assumption unless the loudspeaker came with a polar plot. So the estimate is still an order of magnitude arrived at deliberately rather than a setting computed from data, which is the right status for it.

\paragraph{What actually decides it.} None of the above replaces the switch. A curve chosen from the room's numbers is a starting point to load and then judge as Section~\ref{sec:targetuse} describes, on programme material you know well, with Engage flicked in and out.

\section{Curve parameters}\label{sec:targetshape}
The target curve is defined through four parameters:
\begin{itemize}[nosep]
  \item \textbf{Tilt}: the total fall from \SI{20}{\hertz} to \SI{20}{\kilo\hertz}, in dB. Negative falls, which is the useful direction; 0 aims at a flat in-room response, as before.
  \item \textbf{Turnover}: keep the target flat below this frequency and spread the whole fall over what is left. "off" runs the tilt straight across the band, which is the Harman shape; a turnover gives the EBU one.
  \item \textbf{Bass} and \textbf{Bass freq}: an optional first-order low shelf under the tilt, for the bass lift the Harman target carries near \SI{105}{\hertz}.
\end{itemize}

\noindent The curve is normalised so that its mean over \SIrange{200}{2000}{\hertz} is \SI{0}{\dB} (the same band the analysis normalises a measured response in section~\ref{sec:levelref}) so a target and a measurement are read against the same zero.

It is then applied as attenuation only. The whole curve is pulled down until its highest point reaches \SI{0}{\dB}, which is the dimmed second line on the plot, and the read-out next to the Engage switch says by how much. The layer can therefore never clip an output.

\section{Usage}\label{sec:targetuse}
The target curve corrects nothing by itself. It shapes what a correction aims at. On a rig that has not been measured, corrected and time-aligned it is a tone control and nothing more. Measure (Chapter~\ref{ch:measure}), correct and align (Chapters~\ref{ch:analysis} and~\ref{ch:groupanalysis}), and then come here to decide how the corrected system should tilt.

The button "Engage" controls a plugin parameter, so a host can automate it and it can be bound to a key, because the way to judge a target curve is to switch it in and out while listening to something familiar, and a curve that is obvious on paper is often inaudible in the room. The switch fades over \SI{50}{\milli\second}, so it does not click.

The curve is applied identically to every output, after that output's own EQ and before its delay. Applying it everywhere equally is what keeps it out of the main-to-subwoofer phase relationship: an identical minimum-phase filter on both sides of a crossover cancels out of the difference between them, where a tilt baked into the mains' correction filters alone would not. EBU Tech 3276 asks for the same thing: "All channels should be adjusted in the same way" \cite{EBU3276}.

A "Dry" measurement bypasses the target curve, as it bypasses everything else on the output; "Output + FIR" and "System" measurements include it (Section~\ref{sec:modes}). So a verification measurement shows the tilt, and a measurement made to design a new correction does not.

\screenshot{target-curves.png}{The factory target curves, all normalised to
\SI{0}{\dB} on the \SIrange{200}{2000}{\hertz} band the analysis uses as its
own reference. Flat is the behaviour before this view existed. The dotted line
is EBU Tech 3276's lower tolerance limit for a measured room response,
\SI{-3}{\dB} to \SI{2}{\kilo\hertz} then falling \SI{1}{\dB} per octave,
drawn for scale: it is a tolerance on what a room does, not a rule about what
to aim at, and only the steepest curve crosses it, by \SI{0.4}{\dB} in the
last octave. Produced by \texttt{doc/figures/target\_curves.py}, which reads
the factory set out of the source so the figure cannot disagree with what the
plugin ships.}

\section{The curve bank}\label{sec:targetbank}
Curves are files, one per curve, in a \texttt{target\_curves} folder beside the preset folder, whose full path is shown under the buttons. Factory curves cannot be changed and are written again if the folder loses them. "Save as..." writes your own beside them, and "Save", "Rename" and "Delete" act on yours only. When the values match none of the stored curves the selector reads "(edited)".

The shape in force is part of the configuration: it is saved with the session and with presets, like the matrix and the outputs. The bank is machine-wide, like the preset folder.

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{Curve} & \textbf{Tilt} & \textbf{Where it comes from} \\
\midrule
Flat                 & \SI{0}{\dB}   & A flat in-room response. \\
Gentle               & \SI{-3}{\dB}  & The cautious end of the range the standards allow. \\
Moderate             & \SI{-4}{\dB}  & The default. \\
Strong               & \SI{-6}{\dB}  & About EBU Tech 3276's \SI{1}{\dB} per octave above \SI{2}{\kilo\hertz}, spread across the band. \\
Harman-like          & \SI{-10}{\dB} & Roughly \SI{1}{\dB} per octave with \SI{+3}{\dB} of bass shelf at \SI{105}{\hertz} \cite{Olive2013}. \\
Flat to \SI{1}{\kilo\hertz} & \SI{-6}{\dB} & The EBU shape: flat, then falling, with the fall spread over the top four octaves. \\
\bottomrule
\end{tabularx}
\end{center}

% \section{How it is built}\label{sec:targetfilter}
% The curve is a cascade of first-order pole/zero sections, and the filter is those same sections discretised, so what the plot shows and what the outputs apply cannot drift apart. Six sections carry the tilt, their poles spread geometrically over the band widened two octaves at each end, each losing an equal share of the fall between its pole and its zero. A seventh filter is the shelf. Two sections pack into one biquad, so a full curve costs four per output.

% That construction is what makes the result a straight line in dB against log frequency: measured against a least-squares line it is straight to \SI{0.011}{\dB} at \SI{-3}{\dB} of tilt and \SI{0.036}{\dB} at \SI{-10}{\dB}, and it delivers the fall it names to within about \SI{1}{\percent} of it --- \SI{0.05}{\dB} short at \SI{-6}{\dB}, \SI{0.09}{\dB} at \SI{-10}{\dB} --- the residual being largest at the edges of the band. A turnover's knee is rounded rather than square, six gentle sections being unable to make a corner, so with one set the fall measured from the turnover itself comes out about \SI{0.1}{\dB} short. Shelving filters, the obvious alternative, are not good enough: one RBJ shelf is \SI{1.2}{\dB} off a straight line at only \SI{3}{\dB} of tilt.

% The discretisation is the only place the plot and the filter differ, and only at the top, where the widened pole spread runs past Nyquist: \SI{0.00}{\dB} to \SI{1}{\kilo\hertz}, \SI{0.04}{\dB} at \SI{5}{\kilo\hertz} and \SI{0.18}{\dB} at \SI{20}{\kilo\hertz} for a \SI{-6}{\dB} tilt at \SI{44.1}{\kilo\hertz}. A turnover, whose slope is steeper, reaches \SI{0.37}{\dB} around \SI{14}{\kilo\hertz}. That is well under audibility, and nowhere near the several decibels the curve exists to put back.
