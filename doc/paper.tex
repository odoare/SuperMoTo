%==============================================================================
%  SuperMoTo paper
%==============================================================================
\documentclass[10pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{siunitx}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xfrac}
\usepackage{hyperref}
\usepackage{parskip}
\usepackage[section]{placeins}

\title{\textbf{SuperMoTo: an integrated monitoring matrix, room correction
       and Ambisonics decoding plugin for audio production and immersive
       listening rooms}}

\author{Olivier Doaré\\
  \small LMI, ENSTA Paris, Institut Polytechnique de Paris\\
  \small \texttt{olivier.doare@ensta.fr}
}

% \date{}

\begin{document}
\maketitle

%----------------------------------------------------------------------
\section*{Abstract}
%----------------------------------------------------------------------

SuperMoTo is an open-source audio plugin (VST3, AU and Standalone) that puts a studio's monitoring signal chain into one plugin instance. From that instance it routes up to 32 inputs to 32 outputs through a configurable gain-and-EQ matrix, applies per-loudspeaker FIR correction filters derived from in-room acoustic measurements, time-aligns the loudspeakers of a rig and phase-aligns a subwoofer to the corrected main speakers, and generates periphonic Ambisonics decoders up to third order for immersive listening rooms. Acoustic measurement tools and a transfer-function analysis and correction design engine are built in, so the whole calibration workflow happens inside the plugin, one loudspeaker at a time or for a whole rig in a single pass. Measured on a stereo rig with one subwoofer at ten listening positions, through eight filter variants played side by side, the subwoofer alignment held the level through the crossover to within \SI{0.03}{\dB} when its design delay was shortened by \SI{14}{\milli\second}, where a minimum-phase correction lost \SIrange{2.2}{2.3}{\dB}, and confining the phase correction to the crossover region removed \SIrange{14}{24}{\dB} of pre-echo from \SI{500}{\hertz} to \SI{8}{\kilo\hertz} without changing the summation. It is implemented in C++ using the JUCE framework and is licensed under the AGPL~3.0, or under commercial terms for holders of a commercial JUCE licence.

%----------------------------------------------------------------------
\section{Introduction}\label{sec:intro}
%----------------------------------------------------------------------

Decisions about balance, timbre and spatial imagery are all made at the listening position, so a systematic error in what the engineer hears accumulates through the whole mix. In a real room, what the engineer hears is never the loudspeaker alone but the combined response of the loudspeaker, the room, and any bass-management or routing in the signal path. Three independent classes of error contribute to this. Room modes below the Schroeder frequency produce large, position-dependent peaks and dips with audible ringing, speaker-boundary interference and early reflections add comb-filtering through the midrange, and the loudspeaker's own magnitude and phase response is rarely flat \cite{Toole}. Electronic correction cannot cure all of these (room cancellation notches and late reflections are not minimum-phase and cannot be inverted by any causal filter~\cite{NeelyAllen}), but the broad, reproducible, minimum-phase portion of the combined loudspeaker-room response can be usefully attenuated with a well-designed FIR filter, which reduces coloration and improves translation to other playback systems.

No single open-source tool covers this workflow end to end. The widely-used Room EQ Wizard (REW)\footnote{\url{https://www.roomeqwizard.com/}} provides acoustic measurement and filter design as a standalone desktop application but not as a DAW plugin, requiring the resulting correction impulse responses to be loaded into a separate convolution plugin and routing to be managed outside the tool. Commercial room correction products such as Sonarworks SoundID Reference\footnote{\url{https://www.sonarworks.com/soundid-reference}} and IK Multimedia ARC\footnote{\url{https://www.ikmultimedia.com/products/arcstudio/}} focus exclusively on correction and do not integrate routing, bass management, or immersive audio decoding. High-channel routing tools and Ambisonics decoders exist as separate plugins, the IEM Plug-in Suite\footnote{\url{https://plugins.iem.at/}, GPLv3, from the Institute of Electronic Music and Acoustics, Graz.} being the reference open-source example, but are not combined with a calibration workflow. SuperMoTo covers the whole workflow in one plugin instance, namely the monitoring matrix, the measurement engine, the correction design, the FIR convolution and the Ambisonics decoder generation. It can also read the decoding matrices exported by IEM's AllRADecoder, so an all-round decoder can be driven by a set of measured, individually corrected loudspeakers. The combination matters most in VR and immersive audio production, where a loudspeaker array has to be routed, time-aligned, individually corrected and driven by a decoded Ambisonics signal at the same time.

Aligning a subwoofer's phase to the mains is itself established practice rather than a new idea, and Section~\ref{sec:subalign} places the construction used here against the parametric all-pass designs it descends from. The contributions of this paper are the following.

\begin{itemize}
  \item A monitoring chain that carries routing, per-loudspeaker EQ, time alignment, FIR correction and Ambisonics decoding in one plugin instance, with a latency budget that keeps a rig aligned exactly as the user set it for the least added latency (Section~\ref{sec:latency}).
  \item A closed-form subwoofer phase alignment, read directly off the measured phase and carried by the correction filter that is already there, in place of a parametric all-pass fitted by an optimiser, as done by other solutions (Section~\ref{sec:subalign}). Measured, it makes the main-to-subwoofer summation independent of the delay it is given (Section~\ref{sec:validation}).
  \item A power-weighted crossover-band group-delay estimator for the main-to-subwoofer offset, and the identity that makes one such offset per loudspeaker correct across a whole rig sharing a single subwoofer (Sections~\ref{sec:subalign} and~\ref{sec:group}).
  \item A linear-phase correction whose phase is kept only through the crossover region, where the alignment needs it, and is minimum-phase above; measured, this removes the pre-echo a full phase inversion puts ahead of transients and costs the summation nothing (Sections~\ref{sec:correction} and~\ref{sec:validation}).
  \item An all-in-one open-source implementation in which the measurement, the analysis, the filter design and the playback chain are the same instance, so a rig can be calibrated without leaving it (Section~\ref{sec:impl}).
\end{itemize}

%----------------------------------------------------------------------
\section{The monitoring chain}\label{sec:chain}
%----------------------------------------------------------------------

The routing matrix connects up to 32 inputs to 32 outputs through a grid of crosspoint \emph{frames}. Six independent configurations (A--F) are stored per preset and can be engaged in exclusive mode (instant A/B switching between monitoring rigs) or summed. Everything the plugin does to the audio happens in one pass over the block, in the order the signal physically travels. With $X_i$ the inputs and $\omega = 2\pi f$, the signal reaching output $o$ is
\begin{equation}
  Y_o(\omega) \;=\; g\;
    \underbrace{e^{-j\omega k_o/f_s}}_{\text{latency comp.}}\;
    \underbrace{\tau_o\,E_o(\omega)\,T(\omega)\,e^{-j\omega \delta_o/f_s}\,C_o(\omega)}_{\text{loudspeaker }o}\;
    \sum_{c\,\in\,\mathcal{A}}\;\sum_{i}\;
    \underbrace{\gamma_{cio}\,B_{cio}(\omega)}_{\text{crosspoint}}\;X_i(\omega),
  \label{eq:chain}
\end{equation}
where $\mathcal{A}$ is the set of engaged configurations, $k_o$ and $\delta_o$ are delays in samples at the sampling rate $f_s$, $T(\omega)$ is an optional monitor target curve shared unmodified by every output, and $g$ is the master gain (level, mute and dim on one smoother).

A crosspoint contributes a gain and polarity $\gamma_{cio}$, ramped linearly over \SI{50}{\milli\second} so that switching a route, or a whole configuration, cannot click, followed by its own two-band EQ $B_{cio}$, which is where a configuration-specific tweak on that one route belongs. The per-output terms are the trim and polarity $\tau_o$, a second two-band EQ $E_o$, the target curve $T$, a delay of $\delta_o$ samples (up to \SI{100}{\milli\second}) for physical speaker time-alignment, and the FIR correction $C_o$, applied by a zero-latency partitioned convolution engine \cite{Gardner1995}. Each EQ band is a peaking biquad, or a lowpass, highpass or bandpass of second or fourth order, a fourth-order section being two biquads carrying the Butterworth $Q$ pair scaled by the band's own $Q$ setting.

The FIR correction $C_o$ is designed to flatten the measured response, which is a target in itself: a loudspeaker corrected flat at the listening position sounds harsh, because its directivity index rises with frequency \cite{ITU1116} and the flattened measurement has therefore lifted the direct sound in the treble by the amount the room's reverberant field would otherwise have rolled off. $T$ is where that is put right: a gentle downward tilt from \SI{0}{\dB} at \SI{20}{\hertz} to a user-chosen fall (typically \SIrange{-3}{-6}{\dB}) at \SI{20}{\kilo\hertz}, picked from factory curves informed by EBU Tech 3276's operational room response mask \cite{EBU3276}, ITU-R BS.1116-3 \cite{ITU1116} and the Harman in-room preference target \cite{Olive2013,Toole2015}, or defined by the user as a straight tilt, an EBU-like turnover, or a low-shelf bass lift. Because $T$ carries no output index $o$, it is identical on the mains and the subwoofer, so it cancels out of the phase relationship the alignment of Section~\ref{sec:subalign} depends on, where a tilt folded into each output's own correction filter $C_o$ instead would not.

The delay is rounded to a whole number of samples and read from a ring buffer at one tap, so that it is a pure sample shift and the same operation on every output. The half sample given up is \SI{11}{\micro\second} at \SI{44.1}{\kilo\hertz}, \SI{4}{\milli\metre} of path, and one sample of relative error between two outputs places its first cancellation notch at $f_s/2$, so it cannot comb inside the band, where interpolating between two taps would colour the treble by where the delay falls between samples.

Everything that describes a physical loudspeaker therefore multiplies once, after the sum, and is shared by every configuration (A...F) that drives it, while the routing and its per-route tweaks belong to one configuration. When filters of different lengths are loaded on different outputs, automatic inter-output latency compensation keeps every speaker time-aligned at the listening position for the least added latency (Section~\ref{sec:latency}).

A configuration tool generates standard stereo and surround layouts (2.0 through 7.1) with bass management and writes a complete matrix configuration ready for refinement. The subwoofer is fed either from a dedicated input (an LFE channel) or from the sum of the main channels, and bass management is written as a highpass on each main output and a lowpass on the subwoofer's.

%----------------------------------------------------------------------
\section{Latency and time alignment}\label{sec:latency}
%----------------------------------------------------------------------

The partitioned convolution engine adds no block latency whatever the filter length, but a linear-phase correction is centred on its own impulse, so output $o$ carries a bulk latency $\ell_o$ equal to the position of the peak of its loaded impulse response (about half the filter length, and near zero for a minimum-phase filter). Filters of different lengths on different outputs would therefore introduce timing inconsistencies between them. Writing $d_o$ for the delay the user (or automatic analysis) set on output $o$ in samples and $(\cdot)_+$ for the positive part, the delay line actually runs only the remainder $\delta_o = (d_o-\ell_o)_+$, the uniform compensation is
\begin{equation}
  k \;=\; \max_{o\ \mathrm{fed}}\;\bigl(\ell_o - d_o\bigr)_+ ,
\end{equation}
and each fed output takes the share $k_o = d_o + k - \max(d_o,\ell_o)$ of it, so that
\begin{equation}
  \delta_o + \ell_o + k_o \;=\; d_o + k
  \qquad \text{on every fed output.}
\end{equation}
The rig is aligned exactly as wanted, offset by a single common term $k$, which is the smallest value satisfying $d_o + k \ge \ell_o$ everywhere and is zero whenever each output's own alignment delay already covers its own filter, the usual case for a rig that has been time-aligned. An output that no engaged configuration feeds is left out of the maximum, so an unused loudspeaker's long filter cannot impose latency on the rig.

%----------------------------------------------------------------------
\section{Ambisonics decoding}\label{sec:ambi}
%----------------------------------------------------------------------

The configuration tool also generates periphonic (3D) Ambisonics sampling decoders for orders 1 through 3, following the AmbiX convention (ACN channel order, SN3D normalisation) \cite{NachbarAmbiX2011}. For each loudspeaker at azimuth $\theta$, elevation $\varphi$ and radius $r$, the decode gain is
\begin{equation}
  D[s][j] \;\propto\; a_{n(j)}\,Y_j^{\mathrm{SN3D}}(\Omega_s),
\end{equation}
where $a_n$ are the max-$r_E$ per-degree weights derived from the largest root of the Legendre polynomial $P_{N+1}$ \cite{ZotterFrank2019}, which improves high-frequency localisation stability. The rig is described by a loudspeaker count and a table of azimuth, elevation, radius and output channel, starting either from a hand-tuned regular layout for the order's canonical count or from a near-uniform spherical Fibonacci lattice for any other count. Non-equidistant speaker rigs are compensated by per-speaker delay and trim derived from the inverse-square law, each of which can be left out so that an alignment or a level already obtained from measurements survives a later re-application of the decoder. Bass management low-passes the omnidirectional $W$ component to a subwoofer and high-passes the speaker feeds.

Irregular and hemispherical rigs are better served by an all-round decoder than by a plain sampling decoder, so the tool can instead import the \texttt{.json} configuration exported by the IEM AllRADecoder and use its matrix, converting it to the plugin's SN3D/ACN feed and applying the same radius compensation.

%----------------------------------------------------------------------
\section{Acoustic measurement}\label{sec:measurement}
%----------------------------------------------------------------------

The measurement view drives the plugin outputs with a band-limited stimulus (\SIrange{5}{30}{\second} duration) and records the microphone response on one of the plugin's input channels. The sweep stimulus is a \emph{synchronized} exponential sweep in the sense of \cite{Novak2015}, its sweep rate being quantised so that the stimulus phase at each harmonic arrival time is an exact multiple of $2\pi$, which is what allows the distortion orders to be separated with their true phase at analysis time (Section~\ref{sec:correction}). White noise is offered as the alternative stimulus.

Each captured stereo file stores the raw stimulus on channel 1 and the microphone on channel 2, so the transfer function estimate at analysis time is always the ratio of the captured microphone signal to the sent stimulus. Three modes are provided, "Dry" (bare loudspeaker and room, correction bypassed), "FIR" (single output with its correction in circuit, for before and after verification), and "System" (complete engine including routing and all FIR filters, for end-to-end validation).

A run measures every selected channel in turn with the microphone in one place and writes its files into a measurement folder that numbers positions per channel automatically and tags an optional subwoofer channel. The folder carries a generated XML manifest recording, for every run, its time, its stimulus parameters, a free-text comment, the microphone calibration in effect (name and a verbatim copy of the file) and the dB SPL calibration if one has been made by reading a real sound level meter. The folder is therefore self-describing, and the group-alignment mode of Section~\ref{sec:group} loads one in a single step. Microphone frequency-response calibration files in the REW/miniDSP plain-text format are supported and are applied at analysis time rather than baked into the recordings, so the calibration can be updated, or replaced by the copy embedded in a folder measured years earlier, without re-measuring.

%----------------------------------------------------------------------
\section{Transfer-function estimation and correction design}\label{sec:correction}
%----------------------------------------------------------------------

For each set of measurements (one speaker, multiple microphone positions), the plugin offers two estimators for the loudspeaker-room transfer function. The first is the Welch $H_1$ estimator \cite{Welch1967}
\begin{equation}
  \hat H(f) = \frac{\hat P_{xy}(f)}{\hat P_{xx}(f)},
\end{equation}
with Hann-windowed 50\,\%-overlapped segments and a user-selectable window length of 16\,384 to 262\,144 samples, and it applies to any stimulus. The second, available and selected by default when the run's manifest identifies it as a synchronized sweep, deconvolves the recording by the analytic spectral inverse of the sweep \cite{Novak2015}, which yields the full-band response from a single sweep and requires no regularised division by a measured spectrum. Because a distortion product of order $m$ is emitted $L\ln m$ seconds earlier than the linear response at the same frequency ($L$ being the sweep rate), the deconvolved response separates the orders along the time axis. The linear impulse response is windowed out for the estimate, and orders 2 to 5 are retained as distortion curves, power-averaged across positions. Under the Welch estimator those same products are instead folded into the linear estimate. The two methods share the entire downstream chain, so a set can be re-analysed either way for comparison.

Before averaging across positions, the bulk propagation delay of each measurement is estimated from the impulse response peak and removed, so position-dependent phase ramps do not cancel in the spatial average. The spatial average follows the practice of correcting the broad trend common to the listening area rather than a single-seat interference pattern \cite{Toole, Mourjopoulos1994}. Its magnitude is the power mean over the positions, and its phase the argument of their complex mean, blended towards the minimum phase of that magnitude as the positions stop agreeing: the weight is the debiased vector coherence $\bigl(N R^2-1\bigr)/(N-1)$, clipped to $[0,1]$, of the $N$ positions, $R$ being the modulus of their complex mean over their RMS modulus.

The correction filter is the regularised complex inverse of the frequency-dependent-smoothed average. A variable fractional-octave smoothing is applied, where the octave fraction interpolates log-linearly from an independent low-frequency setting to a high-frequency setting, allowing the correction to track room modes finely at low frequencies while responding only to broad trends above a few kilohertz \cite{Hatziantoniou2000}. Boosts are limited by a smooth $\tanh$ soft-knee ceiling to avoid ringing and headroom loss at room cancellation notches \cite{Kirkeby1999}, and the correction fades back to unity outside a user-set analysis range, where neither the loudspeaker nor the measurement carries usable information.

For subwoofer-equipped systems, a second set of measurements (the sub at the same microphone positions) is loaded, and the phase of the main's correction is steered onto the subwoofer's across the crossover region by an all-pass factor, so that the two sum coherently there. That design is developed in Section~\ref{sec:subalign}.

The final correction spectrum is exported as a mono 32-bit float impulse-response wav file in either \emph{linear-phase} (energy centred at $N/2$, enabling phase correction and subwoofer alignment at the cost of $N/2$ samples of latency) or \emph{minimum-phase} (real-cepstrum method, magnitude-only correction with essentially zero bulk latency, suited to tracking sessions) form \cite{OppenheimSchafer}.

By default the linear-phase export keeps its designed phase only through the crossover region. With $\hat c = C/|C|$ and $\hat m$ the unit phasor of the minimum-phase response of magnitude $|C|$, it exports
\begin{equation}
  C_{\mathrm{lin}}(f) = |C(f)|\,
     \frac{w_p(f)\,\hat c(f) + \bigl(1-w_p(f)\bigr)\,\hat m(f)}
          {\bigl|\,w_p(f)\,\hat c(f) + \bigl(1-w_p(f)\bigr)\,\hat m(f)\,\bigr|},
  \label{eq:phaselimit}
\end{equation}
where $w_p=1$ up to $2f_x$, falls as a raised cosine in $\log_2 f$ to $0$ at $4f_x$ and stays there, $f_x$ being the crossover. The magnitude is untouched, and from $4f_x$ up the export is the minimum-phase one. Only the part of the average's phase that every seat shares can be inverted everywhere \cite{NeelyAllen, Mourjopoulos1994}; a linear-phase filter puts the part a seat does not share \emph{ahead} of the direct sound, as a pre-echo. Above the crossover region nothing needs that phase, since the subwoofer alignment of Section~\ref{sec:subalign} is confined to it. The limit was added after an unlimited export, heard on impact sounds, produced a short pre-reverberation that its minimum-phase twin did not, and Section~\ref{sec:validation} measures both effects: what the limit removes and what it leaves unchanged.

%----------------------------------------------------------------------
\section{Subwoofer phase alignment}\label{sec:subalign}
%----------------------------------------------------------------------

A main loudspeaker and a subwoofer only sum to a flat response through the crossover when they are in phase there \cite{Linkwitz1976}. A fourth-order Linkwitz-Riley lowpass contributes a group delay of $2\sqrt{2}/\omega_x$ at DC and at its corner, \SI{6.4}{\milli\second} at $f_x=\SI{70}{\hertz}$, and the low-frequency acoustic centre of a loudspeaker moves with frequency as well \cite{Vanderkooy2006}. Several milliseconds of the offset are therefore built into the system, and no repositioning of the box removes them.

Steering a main's phase onto the subwoofer's through the crossover has been proposed by a few authors or constructors. Audyssey's phase equalisation cascades second-order all-pass sections onto the satellite channel and computes their pole angles and radii recursively, so as to minimise $\varphi_{\mathrm{sub}}-\varphi_{\mathrm{sat}}-\varphi_{\mathrm{AP}}$ over a weighted band around the crossover \cite{Bharitkar2010, BharitkarBook}, and Dirac Live Bass Control applies a pre-specified number of all-pass biquads to the high- and low-frequency branches so that the subwoofer and the channel are in phase around the crossover at a chosen position in the room \cite{DiracBass}. The crossover region remains an active subject \cite{Kim2025}. In each of these the all-pass is a parametric filter of chosen order whose coefficients are found by an optimiser against a phase-error criterion. What follows shares that objective and differs in construction. The alignment is read straight off the measured phase in closed form, on the same frequency grid the correction is designed on, so there is no filter order to choose, no objective function and no convergence to check, and it is carried by the FIR that already applies the magnitude correction, so it costs nothing at run time.

Aligning the two first requires their relative timing to survive the analysis, which by default it does not. The bulk propagation delay of each measurement is estimated from its impulse-response peak and removed before the positions are averaged, since otherwise the position-dependent phase ramps cancel in the average. Each subwoofer capture is instead delay-anchored on the main capture taken at the same position, shifted by the main's delay rather than by its own, so the averaged subwoofer response $\bar S$ keeps its true phase relative to the main's average.

The alignment itself is a single factor applied to the correction spectrum. From the subwoofer's unit-magnitude phasor $\hat s = \hat S/|\hat S|$, where $\hat S(f) = \pm\,\bar S(f)\,e^{\,j2\pi f T}$ carries the subwoofer polarity and an assumed bulk delay $T$ discussed below, the correction $C$ designed for the main is multiplied by
\begin{equation}
  A(f) \;=\; \frac{\bigl(1-w_x(f)\bigr) + w_x(f)\,\hat s(f)}
                  {\bigl|\bigl(1-w_x(f)\bigr) + w_x(f)\,\hat s(f)\bigr|},
  \label{eq:allpass}
\end{equation}
where the alignment weight is a raised cosine in log frequency,
\begin{equation}
  w_x(f) \;=\; \tfrac{1}{2} + \tfrac{1}{2}\cos\bigl(\pi\, x(f)\bigr),
  \qquad
  x(f) \;=\; \min\!\left(1,\ \max\!\left(0,\ \frac{1}{N_{\mathrm{oct}}}\log_2\frac{f}{f_x}\right)\right),
  \label{eq:alignweight}
\end{equation}
so $w_x=1$ at and below the crossover $f_x$ and $w_x=0$ at and above $2^{N_{\mathrm{oct}}} f_x$, with a release width $N_{\mathrm{oct}}$ of one octave by default. Above the release the main dominates and keeps the phase its own correction gives it.

Three properties of \eqref{eq:allpass} are what make it usable. Taking the phasor discards the subwoofer's magnitude, so none of the subwoofer's own response leaks into the main's correction. The renormalisation makes $|A|=1$ at every bin, so the alignment is strictly a phase operation and leaves the designed magnitude correction untouched, which is what allows the same design to serve with and without a subwoofer. And the blend is taken on the phasors rather than on an unwrapped phase angle, so the interpolation travels the short arc between $1$ and $\hat s$ and the rotation $A$ applies is bounded by $\pi$ at every frequency, by construction and with no phase-unwrapping step, which would need an anchor in a band where the subwoofer is often below the noise. Scaling an unwrapped angle, anchored at the crossover, turns out to be equivalent once the bulk delay below has been removed, and worse when it has not.

The delay $T$ is what keeps the all-pass small. Advancing the subwoofer phasor by $T$ before its phase is taken leaves the all-pass carrying only the residual, while the bulk of the offset is applied as an ordinary delay on the main's output, where it costs nothing. $T$ is a relative quantity, not the delay applied to the main. If the outputs receive bulk delays $a_m$ and $a_s$, the phase difference left between the two acoustic paths through the crossover region is $2\pi f\,[\,T-(a_m-a_s)\,]$, which vanishes at every frequency only if
\begin{equation}
  T \;=\; a_m - a_s .
  \label{eq:aligncond}
\end{equation}
The group-alignment mode designs each speaker's correction with exactly its own $a_i - a_s$, so \eqref{eq:aligncond} holds for every speaker of the rig and for any trim applied to the subwoofer against the group.

$T$ is estimated from the measurements themselves, and the estimator has to be chosen with care, because the two available measures of lateness are not measuring the same quantity. An arrival time read from an impulse-response peak is accurate to about the inverse of the measured bandwidth \cite{Knapp1976}, some \SI{50}{\micro\second} for a full-range loudspeaker but \SI{10}{\milli\second} for a subwoofer that passes nothing above \SI{100}{\hertz}, so it is weakest exactly where it is wanted. The plugin instead fits the slope of the anchored subwoofer's unwrapped phase over $[\,f_x/2,\,2f_x\,]$ by least squares weighted by the measured power $|\bar S|^2$, which returns the effective group delay of the subwoofer in the crossover region, geometry and filtering together, and that is what decides whether the two sources add or cancel. Weighting each point of that fit by $|\bar S|^2$ lets the part of the band where the subwoofer actually has output decide the slope, which is what makes the estimate stable.

Because $A$ carries phase, the correction is a mixed-phase filter and is not causal on its own, which is why the exported impulse response is centred at $N/2$ and why it brings the bulk latency the compensation of Section~\ref{sec:latency} then absorbs. $A$ is the identity from $2f_x$ up, so the phase limit~\eqref{eq:phaselimit}, which starts at $2f_x$, leaves it whole. A minimum-phase export rebuilds the filter from $|C|$ alone and $A$ has unit magnitude, so the subwoofer alignment disappears from it entirely. The loudspeakers still arrive together in that mode, since the delays are written to the outputs regardless, but there is no phase steering through the crossover.

%----------------------------------------------------------------------
\section{Multi-speaker group alignment}\label{sec:group}
%----------------------------------------------------------------------

For rigs of more than a couple of loudspeakers, a group-analysis mode holds one transfer-function estimate per speaker (up to 16, plus one shared subwoofer when the rig has one) side by side, sharing a single set of correction-design settings across all of them. A whole measurement folder is loaded in one step, the manifest supplying which channels were measured, which one is the subwoofer and which output each speaker sits on. Because every channel of a run was measured with the microphone in one place, each subwoofer capture is paired with the speaker capture of the same run, which keeps the pairing correct even when a speaker missed a run. Individual runs can be excluded from the analysis, and runs measured through an existing correction are excluded by default, since a correction has to be designed from the uncorrected loudspeaker.

Each speaker's bulk propagation delay, estimated as in Section~\ref{sec:correction}, is compared across the group and every driver is delayed to match the most distant one, so the whole rig arrives coherently at the reference position. The resulting delay is fed back into each speaker's own subwoofer phase-alignment term before its correction filter is finalised. A level-matching trim per speaker is also derived, as the mean power of its corrected response over the \SIrange{500}{2000}{\hertz} band that SMPTE ST 2095-1 specifies for channel level calibration \cite{SMPTE2095}, relative to the quietest speaker of the group. It is reported rather than applied, so that a deliberate level offset is never overwritten.

The subwoofer itself is never given a correction FIR filter. It contributes only its computed alignment delay and polarity (plus eventual gain lowpass order 2/4 crossover filter as given by the config tool or tuned manually in the matrix).

Applying the result exports every speaker's correction impulse response, writes the delays, the filters and the subwoofer's polarity onto the corresponding outputs, and saves a markdown report listing the settings, the files used, the measured and applied delays, the suggested trims and, optionally, the plotted responses. Loading, re-analysing and exporting a whole group run on a background thread pool with progress feedback, so the editor stays responsive with long, multi-position measurement sets across many speakers, and a batch already running survives the editor being closed.

%----------------------------------------------------------------------
\section{Validation}\label{sec:validation}
%----------------------------------------------------------------------

\paragraph{Rig and campaign.} The rig is a two-way stereo pair with one subwoofer in a treated studio. The loudspeakers and the reference position form a triangle with \SI{1.70}{\metre} sides and a \SI{2.55}{\metre} base, and the subwoofer stands on the floor between the loudspeakers, \SI{1.64}{\metre} from the reference position. Each main and the subwoofer were first measured on their own, correction bypassed, at ten microphone positions over the listening area (six spanning \SI{45}{\centi\metre} along the axis and \SI{34}{\centi\metre} across it, four of them repeated \SI{6}{\centi\metre} higher) and once more at one of them with a chair taken out of the room, with one \SI{20}{\second} synchronized sweep per channel. Two group analyses were run on these eleven captures, one crossed over at \SI{70}{\hertz} with the analysis range starting at \SI{40}{\hertz} and one at \SI{100}{\hertz} from \SI{60}{\hertz}, both smoothed from 1/6 octave in the bass to 1/3 in the treble under a \SI{12}{\dB} boost ceiling. They were exported as eight variants. At \SI{70}{\hertz}, with the design delay the estimator of Section~\ref{sec:subalign} returned, $T=\SI{28.8}{\milli\second}$: the linear-phase export at 8192 taps with and without the phase limit~\eqref{eq:phaselimit}, the limited one at 4096 taps, and the minimum-phase export. At \SI{100}{\hertz}: the limited linear-phase and the minimum-phase exports at 8192 taps, each with the estimated $T=\SI{32.2}{\milli\second}$ and with $T=\SI{17.9}{\milli\second}$, set by hand \SI{14.3}{\milli\second} shorter because it made the all-pass rotation smallest on the plugin's phase plots.

All eight were loaded side by side in one matrix, each on three outputs of its own fed from two inputs of its own, and the whole system was measured at the ten positions, the sixteen inputs in turn at each position with \SI{10}{\second} sweeps. Any two variants are therefore compared at the same placement of the microphone, minutes apart. The subwoofer's routing level, fitted on these runs, is \SI{-2.6}{\dB} at \SI{70}{\hertz} and \SI{-0.25}{\dB} at \SI{100}{\hertz}, and its polarity is normal in every variant, the inverted fit being \SI{2}{\dB} RMS worse. One variant, the limited 8192-tap one at \SI{70}{\hertz}, was measured with a main's correction filter left active by mistake on its subwoofer output from an earlier session. The captures showed it before the saved plugin state did: the difference between that variant and the unlimited export, whose mains are the same filter below $2f_x$, is identical on both mains (correlation 0.999 over \SIrange{30}{120}{\hertz}) and worth \SI{-0.8}{\dB} through the crossover band. With the stray filter, read back from the saved state, in the subwoofer's term,~\eqref{eq:predsum} predicts \SI{-0.85}{\dB}. The variant is used as measured above \SI{250}{\hertz}, where the subwoofer plays no part. Below, the unlimited export stands in for it.

By default, the plugin processes the entire sweep at once to get each response. Using Welch averaging on the same sweeps instead creates a wavy pattern (ripple) in the volume. This ripple repeats every 0.81 octaves (which matches the segment hop time) and gets worse as the system's delay increases. In the impulse response it reads as echoes on both sides of the direct sound, some \SI{20}{\dB} above the real ones in the \SIrange{250}{1000}{\hertz} octaves, in every variant alike.

\paragraph{The model.} Each dry subwoofer capture is delay-anchored on the main capture of the same position, so the same propagation delay $d_p$ has been removed from both, and the acoustic sum at position $p$ is exactly
\begin{equation}
  Y_p(\omega) = e^{-j\omega T}\,H_{\mathrm{hp}}(\omega)\,C(\omega)\,H_p(\omega)
              \;\pm\; g\,H_{\mathrm{lp}}(\omega)\,S_p(\omega)
  \label{eq:predsum}
\end{equation}
up to the common factor $e^{-j\omega d_p}$, which changes no magnitude. $H_p$ and $S_p$ are the measured responses of the main and of the subwoofer, $H_{\mathrm{hp}}$ and $H_{\mathrm{lp}}$ the bass-management filters, $C$ the exported correction and $g$ the subwoofer's routing level. Nothing else is modelled. Against all sixteen measured cases (eight variants, two mains, the unwanted stray filter included) the prediction is within \SIrange{0.84}{1.00}{\dB} RMS over \SIrange{35}{250}{\hertz} (Figure~\ref{fig:crossover}), although the dry captures were taken two days before the system runs, and with positionning error of the order of the centimeter. A difference between two variants, which is what the comparisons below are made of, it predicts better still. An offline re-implementation of the analysis reproduces the plugin's sixteen exported filters to \SI{0.02}{\dB} RMS in magnitude from \SI{40}{\hertz} to \SI{16}{\kilo\hertz}. It is used where the campaign could not measure, {\it i.e.} between and beyond the measured delays, with the subwoofer inverted, and with a position left out of the design.

\paragraph{Latency and treble.} The four variants at \SI{70}{\hertz} carry filters whose bulk latency is 4096, 2048 and 0 samples. At every position their mains arrived at the same sample, give or take two samples of peak picking (\SI{0.05}{\milli\second}). The \SI{100}{\hertz} variants arrived \SI{3.40}{\milli\second} later and \SI{10.88}{\milli\second} earlier than those, exactly the differences between their output delays once each is rounded to a whole sample. The phase type leaves the treble alone: from \SI{200}{\hertz} to \SI{10}{\kilo\hertz} the linear- and minimum-phase exports of one design gave the same response to within \SIrange{0.02}{0.05}{\dB} RMS, on both mains and at both crossovers. An earlier campaign on this rig, which included an uncorrected run, measured the minimum-phase correction as applied to within \SI{0.31}{\dB} and \SI{0.33}{\dB} RMS of the designed one over the same band.

\paragraph{Summation through the crossover.} Table~\ref{tab:levels} compares variants two by two through the crossover band, at the same microphone placements, and sets each difference against the one~\eqref{eq:predsum} predicts from the same exported filters. At the estimated delays the all-pass carries \SI{0.2}{\dB} more level through the band than the minimum-phase export at \SI{70}{\hertz}, at all ten positions, and \SIrange{0.4}{0.5}{\dB} more at \SI{100}{\hertz}, at eight. Given the hand-set delay, \SI{14.3}{\milli\second} shorter, the all-pass result moves by \SIrange{0.01}{0.03}{\dB} while the minimum-phase one loses \SIrange{2.2}{2.3}{\dB} at every position, a cancellation that Figure~\ref{fig:crossover}(c) shows at \SI{110}{\hertz}. Each of these differences is predicted to within \SI{0.11}{\dB}.

\begin{table}[ht]
  \centering
  \small
  \begin{tabular}{llrrrrc}
    \toprule
    & & \multicolumn{2}{c}{measured} & \multicolumn{2}{c}{predicted} & \\
    \cmidrule(lr){3-4}\cmidrule(lr){5-6}
    variants & difference & left & right & left & right & positions\\
    \midrule
    \SI{70}{\hertz}, $T=\SI{28.8}{\milli\second}$  & all-pass $-$ minimum phase & $+0.24$ & $+0.18$ & $+0.26$ & $+0.22$ & 10, 10\\
    \SI{100}{\hertz}, $T=\SI{32.2}{\milli\second}$ & all-pass $-$ minimum phase & $+0.41$ & $+0.48$ & $+0.49$ & $+0.59$ & 8, 8\\
    \SI{100}{\hertz}, $T=\SI{17.9}{\milli\second}$ & all-pass $-$ minimum phase & $+2.72$ & $+2.64$ & $+2.81$ & $+2.61$ & 10, 10\\
    \midrule
    \SI{100}{\hertz}, all-pass      & $T=32.2$ $-$ $T=\SI{17.9}{\milli\second}$ & $+0.03$ & $+0.01$ & $+0.03$ & $+0.04$ & \\
    \SI{100}{\hertz}, minimum phase & $T=32.2$ $-$ $T=\SI{17.9}{\milli\second}$ & $+2.34$ & $+2.16$ & $+2.35$ & $+2.07$ & 10, 10\\
    \SI{70}{\hertz}, all-pass       & 4096 $-$ 8192 taps                         & $-0.01$ & $+0.01$ & $-0.03$ & $-0.02$ & \\
    \bottomrule
  \end{tabular}
  \caption{Level through the crossover band $[\,f_x/\sqrt2,\,f_x\sqrt2\,]$, relative
  to \SIrange{500}{2000}{\hertz}: difference between two variants measured
  at the same microphone placements, mean over the ten positions, in dB, against
  the difference~\eqref{eq:predsum} predicts from the same exported filters.
  The last column counts the positions at which the first variant is the
  louder, where the difference is not nil. At \SI{70}{\hertz} the all-pass is
  the unlimited 8192-tap export, the same filter as the limited one below
  $2f_x$.}
  \label{tab:levels}
\end{table}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\textwidth]{figures/crossover-measured.png}
  \caption{Level through the crossover region, relative to \SIrange{500}{2000}{\hertz}
  and averaged over the ten positions, left main (top) and right main (bottom),
  for the aligned linear-phase export (all-pass) and the minimum-phase one:
  measured (solid) and predicted by~\eqref{eq:predsum} from the dry captures and
  the exported filters (dashed). (a) \SI{70}{\hertz}, estimated delay;
  (b) \SI{100}{\hertz}, estimated delay; (c) \SI{100}{\hertz}, delay set by hand.
  The vertical line marks the crossover.}
  \label{fig:crossover}
\end{figure}

\paragraph{The delay.} Figure~\ref{fig:delay} runs the design delay from \SIrange{10}{40}{\milli\second} through the offline chain, with the measured variants on top. The all-pass stays between \SI{-0.24}{\dB} and \SI{-0.29}{\dB} of summation efficiency at \SI{100}{\hertz} and between \SI{-0.34}{\dB} and \SI{-0.97}{\dB} at \SI{70}{\hertz}. Over the same range the minimum-phase design swings with a period of about $1/f_x$, from \SI{-0.22}{\dB} to \SI{-8.4}{\dB} and from \SI{-0.40}{\dB} to \SI{-6.4}{\dB}. The all-pass advantage is not necessarly a better optimum (indeed, at its best delay the minimum-phase design sums as well). Instead, it eliminates the need to determine that delay. The estimator of Section~\ref{sec:subalign} comes within about a millisecond of it here, which already costs the minimum-phase design \SIrange{0.4}{0.7}{\dB}. The ripple left at \SI{70}{\hertz}, \SI{0.6}{\dB} every \SI{10}{\milli\second}, comes from the release band of~\eqref{eq:alignweight}. Near \SI{100}{\hertz}, $w_x\approx\tfrac12$. When the residual phase at this frequency approaches $180^{\circ}$, the numerator of~\eqref{eq:allpass} nearly vanishes, causing a sudden phase jump. Even with a \SI{70}{\hertz} crossover, the subwoofer still contributes significant energy at \SI{100}{\hertz}. Consequently, at $T=\SI{26}{\milli\second}$, the combined response drops by \SI{3}{\dB} in the \SIrange{100}{110}{\hertz} band, while remaining unaffected elsewhere. Inverting the subwoofer polarity across all three measured settings has minimal impact on the all-pass design, maintaining it between \SI{-0.26}{\dB} and \SI{-0.50}{\dB}. In contrast, this inversion degrades the minimum-phase design, dropping its score to between \SI{-1.9}{\dB} and \SI{-6.0}{\dB}. Finally, because the designs were optimized using nine positions and evaluated at a tenth, the fact that every out-of-sample score remains within \SI{0.02}{\dB} of its in-sample value proves a key point, which the fact that the alignment is a robust property of the entire listening area, and not an artifact of the specific measurement positions used to shape it.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\textwidth]{figures/delay-sweep.png}
  \caption{The design delay $T$ swept through the offline chain (lines, mean of
  both mains), with the measured variants (markers, one per main). Top:
  summation efficiency over $[\,f_x/2,\,2f_x\,]$,
  $20\log_{10}|M+S|-20\log_{10}(|M|+|S|)$ weighted by the overlap of the two
  sources, 0~dB being coherent addition; a measured point takes $|M+S|$ from
  the system run and $|M|+|S|$ from the dry captures. Bottom: energy in the
  \SI{125}{\hertz} octave arriving \SIrange{3}{30}{\milli\second} before the
  direct sound, relative to the octave's energy after it.}
  \label{fig:delay}
\end{figure}

\paragraph{What the delay still decides.} The all-pass makes the steady-state sum independent of $T$, not the time response. $T$ sets when the mains speak relative to the subwoofer, which no delay holds back, and therefore how much of the subwoofer's energy arrives ahead of the mains' direct sound. On top of that, the all-pass steers the main onto the subwoofer in linear phase, partly by advancing it. The lower row of Figure~\ref{fig:delay} shows both in the \SI{125}{\hertz} octave, measured and predicted. At \SI{100}{\hertz}, the \SIrange{3}{30}{\milli\second} before the direct sound hold \SI{-4}{\dB} of the energy that follows it with the estimated delay, and \SIrange{-13}{-16}{\dB} with the hand-set one, against \SI{-15}{\dB} and \SI{-31}{\dB} for the minimum-phase export; at \SI{70}{\hertz}, \SIrange{-16}{-17}{\dB} against \SI{-26}{\dB}. The shorter delay has its own price: the subwoofer's octave, \SI{63}{\hertz}, then peaks \SI{17}{\milli\second} after the direct sound at the median seat, against \SI{3.5}{\milli\second} with the estimated delay. The estimator aims at the delay a delay-only alignment needs. The all-pass does not need it, so $T$ is free to be chosen for the time response instead, and whether early energy in the crossover band or late energy in the subwoofer's band is the more audible is a listening question this campaign does not answer.

\paragraph{The phase limit.} Table~\ref{tab:preecho} measures what the limit~\eqref{eq:phaselimit} removes. From \SI{500}{\hertz} to \SI{8}{\kilo\hertz} the unlimited export put \SIrange{14}{24}{\dB} more energy than the limited one in the \SI{30}{\milli\second} before the direct sound, and \SIrange{14}{30}{\dB} more in the last \SI{3}{\milli\second}, in every octave on both mains. The limited export is within \SI{2}{\dB} of the minimum-phase one over the longer window and within \SI{5}{\dB} over the shorter, both approaching the noise floor above \SI{1}{\kilo\hertz}. At \SI{250}{\hertz}, inside the limit's transition from $2f_x$ to $4f_x$, it removes \SIrange{3}{10}{\dB} and leaves \SIrange{14}{16}{\dB} more than minimum phase. The inversion it removes bought no timing. From \SI{500}{\hertz} to \SI{4}{\kilo\hertz} every octave's envelope peaked within \SI{0.2}{\milli\second} of the direct sound at the median position, in every variant and in the dry captures alike, and within \SI{0.3}{\milli\second} at the worst position in every variant. The unlimited export moved no octave from \SI{250}{\hertz} up by more than \SI{0.11}{\milli\second} against the minimum-phase one. Furthermore, this limit does not negatively impact the summation. Indeed, below $2f_x$ the limited and unlimited exports are the same filter, and the limited 4096-tap export sums within \SI{0.01}{\dB} of the unlimited 8192-tap one through the crossover band (Table~\ref{tab:levels}).

\begin{table}[ht]
  \centering
  \small
  \begin{tabular}{lrrrrrr}
    \toprule
    & \SI{250}{\hertz} & \SI{500}{\hertz} & \SI{1}{\kilo\hertz} & \SI{2}{\kilo\hertz} & \SI{4}{\kilo\hertz} & \SI{8}{\kilo\hertz}\\
    \midrule
    \multicolumn{7}{l}{\textit{\SIrange{30}{3}{\milli\second} before the direct sound}}\\
    minimum phase       & $-49/{-50}$ & $-57/{-57}$ & $-64/{-66}$ & $-68/{-67}$ & $-68/{-68}$ & $-67/{-67}$\\
    linear, limited     & $-35/{-34}$ & $-56/{-56}$ & $-64/{-64}$ & $-67/{-67}$ & $-68/{-68}$ & $-67/{-67}$\\
    linear, not limited & $-32/{-24}$ & $-38/{-34}$ & $-45/{-42}$ & $-46/{-47}$ & $-44/{-48}$ & $-50/{-53}$\\
    noise floor         & $-68$     & $-71$     & $-72$     & $-71$     & $-68$     & $-67$\\
    \midrule
    \multicolumn{7}{l}{\textit{\SIrange{3}{1}{\milli\second} before}}\\
    minimum phase       & $-62/{-65}$ & $-56/{-59}$ & $-64/{-63}$ & $-59/{-56}$ & $-74/{-69}$ & $-77/{-61}$\\
    linear, limited     & $-42/{-44}$ & $-56/{-58}$ & $-64/{-63}$ & $-59/{-56}$ & $-69/{-69}$ & $-73/{-61}$\\
    linear, not limited & $-39/{-26}$ & $-28/{-28}$ & $-36/{-33}$ & $-40/{-42}$ & $-39/{-39}$ & $-43/{-47}$\\
    \bottomrule
  \end{tabular}
  \caption{Energy arriving ahead of the direct sound, per octave, relative to
  that octave's energy in the \SI{300}{\milli\second} after it, in dB, through
  causal octave filters so that nothing is moved earlier than it arrived; power
  mean over the ten positions, left/right main, crossover \SI{70}{\hertz},
  8192 taps. The noise floor is the same \SI{27}{\milli\second} window taken
  \SIrange{400}{150}{\milli\second} ahead of the direct sound, beyond the reach
  of any filter here.}
  \label{tab:preecho}
\end{table}

Below $2f_x$ the linear-phase filter still puts energy ahead of the direct sound, \SI{-14}{\dB} in the \SI{63}{\hertz} octave and \SIrange{-16}{-17}{\dB} in the \SI{125}{\hertz} one at \SI{70}{\hertz}, against \SI{-20}{\dB} and \SI{-26}{\dB} in minimum phase. That is the alignment of the previous paragraphs, and only there did the linear phase move an arrival: the \SI{125}{\hertz} octave by \SIrange{0.4}{0.5}{\milli\second} earlier and the \SI{63}{\hertz} one by \SI{0.9}{\milli\second} later.

\paragraph{Filter length.} At \SI{70}{\hertz}, halving the filter to 4096 taps changed the shape of the response through the crossover band but not its level. The shorter filter left a narrow dip at \SI{81}{\hertz}, \SI{-1.5}{\dB} and \SI{-1.8}{\dB} on the two mains, with rises of \SIrange{0.2}{0.6}{\dB} on either side, so that the level averaged over the band moved by \SI{0.01}{\dB} (Table~\ref{tab:levels}). Equation~\eqref{eq:predsum}, given each filter as rendered, predicted that difference curve to \SI{0.04}{\dB} RMS over \SIrange{35}{250}{\hertz}.

\paragraph{Limits.} This is one rig in one room. The comparisons between the all-pass and the minimum-phase exports, between the two delays at \SI{100}{\hertz} and between the phase limit on and off are measured. The delay sweep between and beyond the measured points, the inverted subwoofer and the leave-one-out scores are predictions of~\eqref{eq:predsum}, whose accuracy on this rig Table~\ref{tab:levels} and the measured points of Figure~\ref{fig:delay} establish. No listening test was run: what is measured is energy and time, and the audibility of what remains below $2f_x$ is left open.

The script that produces the tables and figures of this section from the campaign's folder is in the repository named in Section~\ref{sec:impl}.

%----------------------------------------------------------------------
\section{Implementation and availability}\label{sec:impl}
%----------------------------------------------------------------------

%----------------------------------------------------------------------

SuperMoTo is written in C++ and built with CMake against the JUCE framework\footnote{\url{https://juce.com/}}. Its reusable parts (the correction and measurement DSP, the plotting components, the preset system) live in a separate library, FxmeTools\footnote{\url{https://github.com/odoare/FxmeTools}}, shared with the author's other plugins, and the parts that need no JUCE at all are compiled and unit-tested on their own. Per-output FIR convolution uses the WDL non-uniformly partitioned convolution engine\footnote{\url{https://www.cockos.com/wdl/}} \cite{Gardner1995}.

The engine holds no parameters of its own. It pulls them from the shared model whenever the model's version counter changes, under a try-lock that defers the pull to the next block if the editor happens to be writing, so a GUI edit or a state restore can never stall the audio callback, and every update path is written to allocate nothing on the audio thread. Impulse-response files are read, resampled and installed on the message thread under a lock that the audio thread only ever takes with a try-lock, so a load in flight costs a few unconvolved blocks rather than a blocked callback. Meters and the spectrum analyser are fed by lock-free taps placed on each crosspoint and each output.

Continuous-integration builds for Linux, macOS (Apple Silicon universal binary) and Windows are provided by a GitHub Actions workflow triggered by version tags. The plugin is available in VST3, AU, and Standalone formats with a fixed 32-in/32-out discrete channel bus, which sidesteps the inconsistent per-host discrete-channel-count negotiation of JUCE's AU and VST3 wrappers, the active matrix size being an independent, user-selectable setting up to that count. A user manual covering the workflow, the controls and the signal processing behind them is written in LaTeX alongside the source.

The source, the manual, the unit tests and the scripts that produce Tables~\ref{tab:levels} and~\ref{tab:preecho} and Figures~\ref{fig:crossover} and~\ref{fig:delay} are at \url{https://github.com/odoare/SuperMoTo}, under the AGPL~3.0 or commercial terms; the manual, the paper and the scripts themselves are under the LGPL~3.0.

%----------------------------------------------------------------------
\section{Conclusion}\label{sec:conclusion}
%----------------------------------------------------------------------

SuperMoTo puts the parts of a monitoring chain that are usually separate tools into one plugin instance, so that the routing, the per-loudspeaker correction, the time alignment and the Ambisonics decoding of a listening room are described in one place and calibrated without leaving it. The subwoofer alignment is the part that gains most from that arrangement. Because the measurement, the spatial average and the filter design live in the same instance, the alignment can be read straight off the measured phase in closed form and folded into the correction filter, where a tool working from outside the signal chain would have to fit a separate parametric all-pass to it.

What the approach cannot do is bounded by what any causal filter can do in a room. Cancellation notches and late reflections are not minimum-phase and are not inverted here, and the correction is designed for the area the microphone positions span rather than for one specific position. Within those bounds the measurements of Section~\ref{sec:validation} support two claims. The subwoofer alignment makes the summation through the crossover independent of the delay and of the polarity it is given, where a minimum-phase correction depends on both by several decibels. At its best delay the minimum-phase correction does as well, so what the alignment removes is the need to find that delay, not a limit on the optimum (at least on the tested rig). And a room correction's phase belongs in the crossover region. Inverting it above bought no timing on this rig and put \SIrange{14}{24}{\dB} more energy ahead of the direct sound than the minimum phase, which the limit removes at no cost to the summation. The delay the alignment is designed with still decides the low-frequency time response, and choosing it for that is the question the campaign leaves open.

%----------------------------------------------------------------------
\section{Acknowledgements}
%----------------------------------------------------------------------

Generative AI (Claude, Anthropic) was used in the preparation of this work: to improve english in parts of the text, to improve prior art, and to help writing the analysis scripts of Section~\ref{sec:validation}. All measurements were made by the author, who checked the analysis against the plugin's own implementation and against the measured system, verified the cited literature, and takes full responsibility for the content of the paper. Design, modelling, and DSP implementation of SuperMoTo was performed by the author. Threading, GUI, and project managment was worked on with the help of that same AI tool.

%----------------------------------------------------------------------
\begin{thebibliography}{99}
%----------------------------------------------------------------------

\bibitem{Welch1967}
P.~D. Welch, ``The use of fast Fourier transform for the estimation of
power spectra: a method based on time averaging over short, modified
periodograms,'' \textit{IEEE Trans.\ Audio Electroacoust.},
vol.~15, no.~2, pp.~70--73, 1967.

\bibitem{Novak2015}
A.~Novak, P.~Lotton and L.~Simon, ``Synchronized swept-sine: theory,
application, and implementation,'' \textit{J. Audio Eng. Soc.},
vol.~63, no.~10, pp.~786--798, 2015.

\bibitem{Mourjopoulos1994}
J.~N. Mourjopoulos, ``Digital equalization of room acoustics,''
\textit{J.\ Audio Eng.\ Soc.}, vol.~42, no.~11, pp.~884--900, 1994.

\bibitem{NeelyAllen}
S.~T. Neely and J.~B. Allen, ``Invertibility of a room impulse
response,'' \textit{J.\ Acoust.\ Soc.\ Am.}, vol.~66, no.~1,
pp.~165--169, 1979.

\bibitem{Hatziantoniou2000}
P.~D. Hatziantoniou and J.~N. Mourjopoulos, ``Generalized
fractional-octave smoothing of audio and acoustic responses,''
\textit{J.\ Audio Eng.\ Soc.}, vol.~48, no.~4, pp.~259--280, 2000.

\bibitem{Kirkeby1999}
O.~Kirkeby and P.~A. Nelson, ``Digital filter design for inversion
problems in sound reproduction,'' \textit{J.\ Audio Eng.\ Soc.},
vol.~47, no.~7/8, pp.~583--595, 1999.

\bibitem{Linkwitz1976}
S.~H. Linkwitz, ``Active crossover networks for noncoincident drivers,''
\textit{J.\ Audio Eng.\ Soc.}, vol.~24, no.~1, pp.~2--8, 1976.

\bibitem{SMPTE2095}
SMPTE ST 2095-1:2015, \textit{Calibration Reference Wideband Digital Pink
Noise Signal}. White Plains, NY: Society of Motion Picture and Television
Engineers, 2015.

\bibitem{Bharitkar2010}
S.~Bharitkar and C.~Kyriakakis, ``Phase equalization for multi-channel
loudspeaker-room responses,'' U.S. Patent 7\,720\,237~B2, 2010
(priority 7~Sept.\ 2004).

\bibitem{BharitkarBook}
S.~Bharitkar and C.~Kyriakakis, \textit{Immersive Audio Signal Processing}.
New York: Springer, 2006.

\bibitem{DiracBass}
L.-J.~Br\"annmark, F.~Rosencrantz and M.~Ueno Andersson, \textit{Dirac Live
Bass Control}. Dirac Research, technical white paper, 2020.

\bibitem{Kim2025}
J.~Kim, ``Group delay-driven crossover optimization for subwoofer satellite
systems at listening position,'' \textit{Acta Acustica}, vol.~9, art.~51, 2025.

\bibitem{Knapp1976}
C.~H. Knapp and G.~C. Carter, ``The generalized correlation method for
estimation of time delay,'' \textit{IEEE Trans.\ Acoust., Speech, Signal
Process.}, vol.~24, no.~4, pp.~320--327, 1976.

\bibitem{Vanderkooy2006}
J.~Vanderkooy, ``The acoustic center: a new concept for loudspeakers at low
frequencies,'' in \textit{Proc.\ 121st AES Convention}, San Francisco, 2006.

\bibitem{Toole}
F.~E. Toole, \textit{Sound Reproduction: The Acoustics and
Psychoacoustics of Loudspeakers and Rooms}, 3rd ed.\ New York:
Routledge, 2017.

\bibitem{Gardner1995}
W.~G. Gardner, ``Efficient convolution without input-output delay,''
\textit{J.\ Audio Eng.\ Soc.}, vol.~43, no.~3, pp.~127--136, 1995.

\bibitem{OppenheimSchafer}
A.~V. Oppenheim and R.~W. Schafer, \textit{Discrete-Time Signal
Processing}, 3rd ed.\ Upper Saddle River, NJ: Prentice Hall, 2010.

\bibitem{NachbarAmbiX2011}
C.~Nachbar, F.~Zotter, E.~Deleflie, and A.~Sontacchi, ``AmbiX --- a
suggested ambisonics format,'' in \textit{Proc.\ 3rd Ambisonics
Symposium}, Lexington, 2011.

\bibitem{ZotterFrank2019}
F.~Zotter and M.~Frank, \textit{Ambisonics: A Practical 3D Audio Theory
for Recording, Studio Production, Sound Reinforcement, and Virtual
Reality}. Cham: Springer, 2019 (open access).

\bibitem{EBU3276}
EBU Tech 3276, \textit{Listening conditions for the assessment of sound
programme material: monophonic and two--channel stereophonic}, 2nd~ed.
Geneva: European Broadcasting Union, 1998. (Operational room response curve,
\S2.4 and Fig.~2: the tolerance mask lets the response fall at
\SI{1}{\dB} per octave above \SI{2}{\kilo\hertz}, and the notes advise
confining equalisation to below \SI{300}{\hertz} and adjusting all channels
the same way.)

\bibitem{ITU1116}
Recommendation ITU-R BS.1116-3, \textit{Methods for the subjective assessment
of small impairments in audio systems}. Geneva: International
Telecommunication Union, 2015. (\S7.2.2.2: the monitor's directivity index
shall rise smoothly, \SIrange{6}{12}{\dB} over
\SIrange{500}{10000}{\hertz}. \S8.3.4.1 and Fig.~2: the operational room
response tolerance widens at \SI{1.5}{\dB} per octave above
\SI{2}{\kilo\hertz}.)

\bibitem{Toole2015}
F.~E. Toole, ``The measurement and calibration of sound reproducing systems,''
\textit{J. Audio Eng. Soc.}, vol.~63, no.~7/8, pp.~512--541, 2015.

\bibitem{Olive2013}
S.~E. Olive, T.~Welti, and E.~McMullin, ``Listener preferences for in-room
loudspeaker and headphone target responses,'' in \textit{Proc. 135th AES
Convention}, New York, 2013, paper~8994.

\end{thebibliography}

\end{document}
